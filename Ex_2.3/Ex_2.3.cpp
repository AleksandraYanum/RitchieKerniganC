#include <stdio.h>
#include <stdlib.h>

// Program converts hexadecimal digits from string to integer value

#define MAXSIZE 10

int main()
{
	int htoi(char string[]);
	char string[MAXSIZE];
	
	return EXIT_SUCCESS;
}